export default {
  plugins: {
    // The PostCSS plugin for Tailwind was moved to a separate package.
    // Use '@tailwindcss/postcss' when using Tailwind v4+.
    '@tailwindcss/postcss': {},
    autoprefixer: {},
  },
}